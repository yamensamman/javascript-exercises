// Two different graph implementations (adjacency list and adjacency matrix).
// Note that each one is using a different JS object creation pattern. Make
// sure to check out both!

const graphImplementation = { adjList: 0, adjMatrix: 1 };

module.exports = { graphImplementation };